description "{{db_dialect}} ambassador"
author "fupelaqu"
start on filesystem and started {{db_dialect}}
stop on runlevel [!2345]
respawn
script
  /usr/bin/docker start -a {{db_dialect}}_ambassador
end script
